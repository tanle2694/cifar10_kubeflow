apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: kubeflow-user-example-com
spec:
  volumes:
    - name: host-volume
      persistentVolumeClaim:
        claimName: cifar-workspace-read-claim
  containers:
    - image: busybox
      name: busybox
      command: ["/bin/sh"]
      args: ["-c", "sleep 600"]
      volumeMounts:
      - name: host-volume
        mountPath: /mydata